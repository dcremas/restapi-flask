Flask==3.1.0
# psycopg 3 with the connection pool. The pool is the reliability fix: the old
# implementation held one connection forever and never recovered from a restart.
psycopg[binary,pool]==3.2.13
gunicorn==23.0.0
python-dotenv==1.0.1
# Ships the Swagger UI dist so /docs needs no CDN.
swagger_ui_bundle==1.1.0
